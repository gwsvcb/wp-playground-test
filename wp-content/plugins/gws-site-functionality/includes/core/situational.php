<?php

/*
 * Functions for unique or context-specific use cases
 * Applies logic conditionally based on environment, page type, or other runtime factors
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit; // Exit if accessed directly
}

/* Add situational functions here e.g.:
 * 
 * Only load the reCAPTCHA script on certain pages (where there is a webform)
 * WPCF7 redirection to acknowledgement page
 * Etc
 * 
 */